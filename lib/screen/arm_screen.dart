import 'package:flutter/material.dart';

class ArmScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("腕のトレーニング"),
      ),
      body: Center(
        child: Text("腕のトレーニング内容"),
      ),
    );
  }
}
