import 'package:flutter/material.dart';

class legScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("脚のトレーニング"),
      ),
      body: Center(
        child: Text("脚のトレーニング内容"),
      ),
    );
  }
}
